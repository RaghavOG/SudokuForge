# SudokuForge

Web Sudoku with Next.js (App Router) + TypeScript. Puzzles are generated on the fly; no auth or database.

**Run:** `npm install` then `npm run dev` → [http://localhost:3000](http://localhost:3000)

**Layout:** `algorithms/` = pure Sudoku logic (solver, types). `app/api/` = API routes. `components/` = UI.
